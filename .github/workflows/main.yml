name: workflow1
on:
  push:
      branches:
      - main

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: setup python 3.10
          uses: actions/setup-python@v4
          with:
              python-version: '3.10'
  
   
